# ADR 0002: PI Internal Parity Tracking

## Status

Accepted

## Context

ADR 0001 scoped agent4j as a Java port focused primarily on externally
observable PI compatibility. The current direction is stricter: agent4j should
also stay as close as practical to PI's internal harness implementation model.

This does not mean a line-by-line TypeScript translation. It does mean module
boundaries, message shapes, event sequencing, lifecycle hooks, queue semantics,
and conversion points should follow PI unless Java type safety or platform
constraints justify a documented divergence.

## Decision

Each major subsystem must be mapped to its PI source package and behavior before
being marked complete. Intentional divergences must be documented with a reason
and, where practical, a compatibility test.

Primary PI reference packages:

- `@earendil-works/pi-ai`
- `@earendil-works/pi-agent-core`
- `@earendil-works/pi-coding-agent`

Current target version:

- PI coding agent `0.82.x`, currently inspected at `0.82.1`.

## Parity Map

| agent4j area | PI reference | Current status | Notes |
| --- | --- | --- | --- |
| `agent4j-ai` message/content model | `packages/ai/src/types.ts` | Complete for Phase 6, Phase 8 provider metadata baseline complete | Java now has PI-style text, thinking, image, tool-call, user, assistant, system, and tool-result types plus assistant message/text/thinking/tool-call stream start/delta/end/error records. Phase 8 adds `AiModelFeatures` as the typed provider/model capability surface around this message model. |
| `agent4j-ai` provider abstraction | `packages/ai/src/types.ts`, `packages/ai/src/providers/*` | Complete for Phase 8 provider gap baseline | Java now has `AiProvider`, `AiProviderRequest`, `AiProviderContext`, `AiStreamOptions`, request hooks, model metadata, registry/selection, auth-store abstractions, OpenAI Responses and Anthropic Messages adapters, fake/recorded contract tests, timeout transport, and normalized usage/stream events. Phase 8 provider-options baseline is complete with `AiGenerationOptions`, carrying common max-output, temperature, top-p, top-k, tool-choice, parallel-tool-call, and metadata settings into OpenAI/Anthropic request serialization. Timeout/retry ownership is now pinned: `httpIdleTimeoutMs`/`AgentLoopRequest.modelTimeout` flows to provider transport request timeout, `AgentLoopRequest.maxModelRetries` owns model-round retry count, provider calls issued by `AgentLoop` use `AiStreamOptions.maxRetries = 0` to avoid nested adapter retries, and `AiRetryClassifier`/`AiProviderHttpException` classify transient provider failures. Provider/model feature flags are represented by `AiProviderFeatures` and `AiModelFeatures`; adapters expose provider flags and honor model/provider tool-calling, tool-choice, and parallel-tool-call support when serializing requests. Endpoint/base-url parity is pinned: `AiProviderRequest` applies auth `baseUrl` to a request-scoped effective model, while OpenAI/Anthropic adapters resolve endpoints from `request.model().baseUrl()` plus provider path suffixes. Auth-mode readiness is pinned with `AiAuthMode`, access-token/subscription-ready `AiResolvedAuth` fields, OpenAI bearer access-token support, and settings parsing for `authMode`, `accessToken`, `expiresAt`, and auth metadata. Phase 9 owns the live ChatGPT/Codex subscription login flow itself. |
| `agent4j-core` agent transcript | `packages/agent/src/types.ts` | Complete for current phase | Java uses `AgentMessage` as the durable JSONL-compatible transcript envelope, with PI-style typed transcript views equivalent to `AgentMessage = Message | CustomAgentMessages[...]` for internal access. Default/coding converters, AgentLoop tool-call/tool-result paths, and prompt inference consume those views. Decision: do not promote the view hierarchy into the primary Java transcript model until broader PI fixture coverage proves unknown-field compatibility. |
| `agent4j-core` agent loop | `packages/agent/src/agent-loop.ts` | Complete through Phase 8 message-state reduction slice | Current loop supports fake and provider-backed text/tool-call turns, emits PI-style start/update/end event boundaries, emits prompt user `message_start`/`message_end` events before the assistant stream, appends tool-result transcript messages in assistant source order, has configurable sequential/parallel tool execution with parallel as the default, has prompt/steering/follow-up queue drain semantics, and has retry success/exhaustion plus model/tool abort coverage. `turn_end` intentionally repeats the round assistant message and ordered tool-result messages, matching PI summary-event behavior; `agent_end` returns all new messages for the invocation. Java now uses `AgentConversationContext` as the canonical mutable conversation context for model-visible transcript plus generated messages. Provider-facing `AiMessage` input is rebuilt at the model boundary, and assistant-only output is derived from generated messages rather than tracked as separate loop-owned state. |
| `agent4j-core` event model | `packages/agent/src/types.ts` | Complete through Phase 7, payload audit pending | PI-style discriminators and text/tool-call loop ordering are in place: `agent_start`, `turn_start`, `message_start`, `message_update`, `message_end`, `tool_execution_*`, `queue_updated`, `retry_*`, `compaction_*`, `turn_end`, `agent_end`, and `agent_aborted`. Tool updates can publish `tool_execution_update`, and JSON serialization coverage pins queue, retry, tool update/end, turn end, agent end, and abort payloads. Remaining gap is a final PI payload audit for compaction/branch-summary events. |
| `agent4j-core` tool abstractions | `packages/agent/src/types.ts` | Complete for Phase 8 hook timing baseline | Basic registry/executor exists with sequential/parallel execution modes, `ToolContext` update callbacks, loop-level before/after tool hooks, hook-returned blocked tool results, and terminate hints. Hook timing is pinned: hooks run inside the `tool_execution_start` / `tool_execution_end` event window, after-hooks run for normal and blocked results, and transcript tool-result messages are emitted after `tool_execution_end`. Phase 11 still owns exact PI extension hook names, discovery, and hook exception policy. |
| `agent4j-coding` built-in tools | `packages/coding-agent/src/core/tools/*` | Complete for Phase 8 schema/result baseline | Built-in tools expose exact per-tool JSON schemas with property descriptions and no unrelated arguments, successful path-bearing result payloads use workspace-relative paths, and tests pin stable tool order plus read/write/edit/bash/ls/grep/find result keys. Remaining PI-source-specific gaps are exact description text, image read payload shape, edit multi-replacement behavior, and streaming render/update details. |
| `agent4j-coding` custom message conversion | `packages/coding-agent/src/core/messages.ts` | Complete for Phase 8 Java prompt baseline | Core exposes the injectable `convertToLlm` boundary. Java now has a coding-agent converter plus pinned XML-like prompt wrappers for bash execution, custom messages, branch summaries, and compaction summaries; custom message type attributes are escaped and unknown custom roles are skipped. Remaining PI-source-specific gap is exact prompt text comparison and any additional concrete custom message variants. |
| Session JSONL model | `packages/coding-agent/src/core/session-manager.ts`, `packages/agent/src/harness/session/*` | Complete for Phase 8 session slices | Existing codec/manager cover core entries, can append Phase 4+ `AgentMessage` loop outputs as PI-shaped message entries, append full `AgentLoopResult.messages()` for repeated runs as one freshness-checked locked batch, rebuild active-path message entries as `AgentMessage` envelopes for resume, reject stale same-file append snapshots with a reopen-required error, validate append-log parent integrity for resume/import, validate known typed payloads before resume/import/append, persist compaction summaries/retained tail entries, append branch summaries, and test fixtures cover branch/compaction/branch-summary resume, unknown forward-compatible payloads, and malformed typed records. |
| Resource discovery/settings | `packages/coding-agent/src/core/resource-loader.ts`, `settings-manager.ts`, `skills.ts`, `prompt-templates.ts` | Complete for Phase 8 resource/settings cleanup | Java now has a `ResourceLoader` boundary with typed discovery output, PI/global/project resource directories, context/system/append prompt loading, settings merge with unknown-field preservation, prompt/skill/theme discovery, local package resources, resource disable gates, non-interactive project trust gating, `SystemPromptBuilder`, and request preparation that keeps system prompts out of persisted transcripts. The supported keys and remaining later-phase gaps are pinned in `docs/resource-settings-parity.md`. |
| Compaction | `packages/coding-agent/src/core/compaction/*`, `packages/agent/src/harness/*` | Complete for Phase 7, integrated with Phase 8 conversation context | Java now has context usage/status, PI-style cut-point planning, summary prompt serialization, retained tail messages, manual compaction, threshold compaction, overflow recovery, pre-summarization tool-result pruning/tool-call argument truncation, and provider-backed branch summary hooks that persist `branchSummary` transcript messages. Phase 8 moved raw-loop conversation ownership into `AgentConversationContext`; Phase 9 should attach that context to the session runtime API. |
| Parity gap fix phase | PI references listed above | Phase 8 complete for planned slices | Closed provider options/timeout/retry/features/endpoint/auth readiness, session-owned conversation context, message-state reduction, session validation/resume semantics, tool schema/result audit, tool hook timing audit, custom message prompt audit, resource/settings cleanup, and documentation closeout. Remaining PI-source-specific audits are tracked as later-phase work. |
| SDK/runtime API | `packages/coding-agent/src/core/agent-session*.ts`, `sdk.ts` | Phase 9 implementation complete; production verification pending | Must mirror `AgentSession`, `AgentSessionRuntime`, services, new/resume/fork/clone/import responsibilities after Phase 8 closes the conversation ownership gap. Java target ownership is pinned in `docs/sdk-runtime-shape-audit.md`: `agent4j-core` stays generic, `agent4j-coding` owns the coding SDK/runtime, runtime/session APIs own canonical conversation context, and auth/login is a runtime service before CLI wrappers. Java now has initial SDK interfaces and request/response records under `com.agent4j.coding.sdk` for session lifecycle, prompt calls, prompt results, and SDK event subscription. `CodingAgentSessionRuntime.createSession(...)` creates a PI-shaped JSONL session, appends optional session-info/model-change entries, initializes an empty session-owned `AgentConversationContext`, and returns a `CodingAgentSession` handle. `CodingAgentSession.prompt(...)` runs direct model-client loops from session-owned active history, persists full generated message batches, and refreshes session context so repeated prompts do not require caller-rebuilt history. `CodingAgentSessionRuntime.resumeSession(...)` opens persisted JSONL sessions, optionally navigates to a requested active entry, rebuilds session context from the active path, and can continue prompting from resumed or branched history. Import/clone/fork delegate to `SessionManager` validation/copy/active-path behavior and return refreshed session handles. Runtime-wide and session-filtered SDK event subscriptions are pinned through real prompt-flow tests using the existing `AgentEvent` payload hierarchy. `CodingAgentRuntimeServices` centralizes runtime-owned services for events, direct model-client execution, provider-registry execution, tools, message conversion, clock, request preparation, session compaction, branch summaries, and login service. `LoginService` supports API-key/access-token login, status, logout, persisted user credentials, `AiResolvedAuth` resolution, and ChatGPT/Codex subscription browser-login orchestration with callback completion, refresh, cleanup, and status metadata. Provider-backed prompt execution resolves provider auth through `LoginService.resolveAuth(...)` and passes request-scoped `AiResolvedAuth` into `AgentLoop`/`AiProviderRequest`. `OpenAiCodingRuntimeOptions` and `CodingAgentRuntimeServices.withOpenAi(...)` assemble the standard OpenAI provider registry, credential store, and subscription client. Deterministic fake-transport coverage and an opt-in `OpenAiSubscriptionLiveIT` cover the implementation. The Phase 9/auth parity gap is not closed until the configured production endpoints are verified and that live test completes successfully with a real subscription. |
| Production OAuth configuration | OpenAI Codex login profile | Implemented, closure pending production verification | `OpenAiSubscriptionLoginClientOptions.codexDefaults()` pins the Codex client ID, auth/token/device endpoints, localhost callback, connector scopes, ChatGPT Codex API base URL, and authorization parameters; `OpenAiCodingRuntimeOptions` enables it by default. `DefaultLoginService.loginOpenAiSubscription()` owns browser launch, local callback handling, credential persistence, cleanup, and the resulting `AuthStatus`; browser-launcher injection is internal test infrastructure. It bounds waits by OAuth expiry, cleans temporary flows on every one-call exit path, handles OAuth errors, rejects duplicate callbacks, and completes pending waits on server shutdown. Token responses reject blank access/refresh tokens, invalid token types, and invalid expiry data; valid rotated refresh tokens replace the prior token. `OpenAiSubscriptionLiveIT` is opt-in, environment-gated, excluded from normal CI discovery, and covers interactive login through a real provider-backed prompt. `docs/openai-sdk-guide.md` and the runnable `OpenAiSubscriptionSdkExample` document setup, login modes, credential lifecycle, and session use without exposing secrets. Do not close this row until production endpoint configuration is verified and the live test succeeds; exact production device-code protocol parity remains later work. |
| CLI modes | `packages/coding-agent/src/modes/*`, `cli/*` | Phase 10 Slices 1-6 complete; live queue gap remains | `docs/pi-cli-shape-audit.md` pins PI `0.82.1` command/mode selection, argument precedence, stdin/stdout, exit, JSON-event, and RPC contracts before implementation. `agent4j-cli` now has a Picocli root command, injectable environment/runtime factory, and output injection for process-contract tests. `DefaultCliRuntimeFactory` discovers resources/settings, resolves the configured OpenAI model, installs coding tools and Phase 9 credential services, and returns `CodingAgentSessionRuntime` without duplicating loop construction. Print, JSON, and RPC runners now obtain persistent sessions through `CliSessionLifecycle`, which maps create/resume/fork/name/no-session choices into the SDK lifecycle boundary and uses PI's cwd-encoded default session directory. `RpcModeRunner` implements strict JSONL command framing, request-id correlation, parse recovery, asynchronous prompt acceptance, runtime event streaming, state/message/session commands, and shutdown. It currently drains steering/follow-up backlogs after the active SDK prompt because the SDK does not yet expose PI's live queue mutation surface. Remaining divergences are noninteractive resume selection, local-only session ID lookup, no interactive picker/TUI, package-management commands, extension CLI flags, and the incomplete RPC command surface. |
| Extension SPI | `packages/coding-agent/src/core/extensions/*` | Phase 11, not started | Java SPI should mirror PI hook names and timing first, then document Java-specific differences. |
| Interactive shell/TUI | `packages/coding-agent/src/modes/interactive/*` | Phase 12, not started | Mirror interaction model, command names, selectors, queue controls, and status semantics before Java terminal rendering choices. |

## Open Phase 9 Gaps

Phase 9 implementation is complete but the subscription-login parity gap is
not closed. Required evidence is production verification of the configured
OpenAI/Codex OAuth and model endpoints plus one successful, non-secret
`OpenAiSubscriptionLiveIT` execution using a real ChatGPT subscription. Exact
production device-code protocol parity is separately deferred: its generic
client and fake-transport contracts must not be presented as a verified Codex
device-login implementation until the production flow is confirmed.

## Intentional Divergence Log

| Area | Divergence | Reason | Follow-up |
| --- | --- | --- | --- |
| Language/runtime | Java 21 modules instead of TypeScript packages | Project goal is a Java port. | Keep module responsibilities mapped to PI packages. |
| Extension execution | Java SPI first, no native TypeScript extension execution initially | Security and runtime complexity. | Revisit in Phase 13 PI Package Bridge. |
| Tool binary guard | Removed NUL-byte binary rejection from read/edit/grep | PI read decodes non-image files as UTF-8 and does not use this binary rejection. | Add image handling or charset policy only when matching PI behavior. |
| Current AI/core bridge | `agent4j-core.AgentMessage` still stores JSON content while `agent4j-ai` has typed content | Intentional current-phase design to preserve PI session JSONL round-tripping. | Keep using typed transcript views plus `AgentMessageConverter` as the `convertToLlm` boundary. Revisit promotion only after broader PI fixture coverage. |
| Agent loop message ownership | Java keeps `AgentConversationContext` inside the raw loop while PI mutates `AgentState.contextMutable()` | The Java raw loop is still persistence-agnostic until SDK/runtime APIs exist, but the internal shape is now closer to PI: one context owns the working transcript and generated messages, model input is temporary boundary data, and assistant output is derived for result reporting. | Phase 9 SDK/runtime should attach this context to the session runtime instead of reintroducing loop-local canonical state. |

## Completion Rule

A phase is not complete until:

1. The subsystem is mapped to PI reference files.
2. External behavior tests pass.
3. Internal lifecycle/message/event/tool semantics have been audited.
4. Any divergence is recorded in this ADR or a more specific parity document.

## Consequences

This constraint may slow implementation in the short term because design choices
must be checked against PI internals. The benefit is a Java harness that is
easier to reason about as a PI port, easier to compare during future upgrades,
and less likely to drift into an incompatible agent framework.
